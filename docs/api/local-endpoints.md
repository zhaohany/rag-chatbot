# Local API Endpoints

Base URL: `http://127.0.0.1:8000`

说明：当前是教学 skeleton 阶段，schema 尚未定稿，接口先返回最小占位结构。

## GET /health

Response example:

```json
{}
```

## POST /ingest

用途：预留入库流程入口（后续课堂逐步实现）

Request body: none

Response example:

```json
{}
```

## POST /query

Request example:

```json
{}
```

Response example:

```json
{}
```
